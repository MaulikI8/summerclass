from django.contrib import admin

# Cart and CartItem models are purely session/temporary frontend data
# and are hidden from Django admin to keep the admin panel clean and focused on Orders & Products.
